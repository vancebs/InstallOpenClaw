# InstallOpenClaw

# 安装
```shell
# 下载
git clone https://github.com/vancebs/InstallOpenClaw.git
cd InstallOpenClaw

# 编辑env文件
cp env_sample env
vim env

# 安装
./install.sh
```

**如果要强制覆盖已安装的brew和node.js请用以下命令安装**
```shell
./install.sh -f
```
